{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "mncs-fabric.work-item.v0.1",
  "type": "object",
  "required": ["schema_version", "job_identity", "partition_identity", "bundle_identity", "consumer_context_identity", "placement_request_identity", "replica_count", "work_item_identity"],
  "properties": {
    "schema_version": {"const": "mncs-fabric.work-item.v0.1"},
    "job_identity": {"type": "string"},
    "partition_identity": {"type": ["string", "null"]},
    "bundle_identity": {"type": ["string", "null"]},
    "consumer_context_identity": {"type": ["string", "null"]},
    "placement_request_identity": {"type": ["string", "null"]},
    "replica_count": {"type": "integer", "minimum": 1, "maximum": 64},
    "work_item_identity": {"type": "string"}
  }
}
