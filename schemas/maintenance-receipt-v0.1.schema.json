{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "mncs-fabric.maintenance-receipt.v0.1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "operation_id", "worker_identity", "controller_identity", "mode", "plan_identity", "desired_state_identity", "inventory_before_identity", "inventory_after_identity", "actions", "certification_identity", "disposition", "failure_class", "final_management_state", "created_at", "claim_boundary", "receipt_identity"],
  "properties": {
    "schema_version": {"const": "mncs-fabric.maintenance-receipt.v0.1"},
    "operation_id": {"type": "string"},
    "worker_identity": {"type": "string"},
    "controller_identity": {"type": "string"},
    "mode": {"enum": ["plan", "apply"]},
    "plan_identity": {"type": "string"},
    "desired_state_identity": {"type": "string"},
    "inventory_before_identity": {"type": "string"},
    "inventory_after_identity": {"type": ["string", "null"]},
    "actions": {"type": "array"},
    "certification_identity": {"type": ["string", "null"]},
    "disposition": {"enum": ["PASS", "FAIL", "UNKNOWN", "NO_CHANGES"]},
    "failure_class": {"type": ["string", "null"]},
    "final_management_state": {"type": "string"},
    "created_at": {"type": "string"},
    "claim_boundary": {"type": "string"},
    "receipt_identity": {"type": "string"}
  }
}
